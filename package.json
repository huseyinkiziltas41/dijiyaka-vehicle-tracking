{
  "name": "philip-morris-vehicle-tracking",
  "version": "1.0.0",
  "description": "Vehicle tracking system for Philip Morris factory",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "client": "cd client && npm start",
    "build": "cd client && npm install && npm run build",
    "render-postbuild": "npm install && cd client && npm install && npm run build"
  },
  "dependencies": {
    "axios": "^1.12.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "socket.io": "^4.7.2",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "keywords": [
    "vehicle-tracking",
    "philip-morris",
    "logistics"
  ],
  "author": "Philip Morris Vehicle Tracking Team",
  "license": "ISC"
}
